name: Run tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  run-tests:
    uses: ronasit/github-actions/.github/workflows/tests.yml@main
    secrets: inherit
